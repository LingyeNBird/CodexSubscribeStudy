# 最终上线范围：仅原始证据

本说明替代旧实验决策中“保留 PF / 平均恒定容量估值辅助通道”的工程建议，不改写此前实验比较和失败记录。

科研上报、协议、中心归因及界面均不发送、不使用粒子滤波或平均恒定容量估值。不用当前 1.8 倍修正所产生的派生容量作科研标签，也不保留估值辅助分析。正常额度监控中的这两种模型保持不变。

当前科研固定 FAST 目标倍率为 2，GPT-5.6/GPT-6 长上下文目标倍率为 1，其他模型按既有正确计费，只估计 GPT-6 四个成本分项倍率。另一项科研可以选择其他待研究变量，但必须采用独立的方法版本，不能自动互相反馈修正。

贡献无本地请求数、周期数、区间数或局部识别门槛。单条请求也能提交；中心合并同一候选参数的证据，不要求每个安装先得出独立结论。原始事实缺失不等于零消耗，无区分信息的贡献不伪造信息量。

对容量漂移的敏感性比较仅基于原始事实和预声明误差模型，绝不包含 PF / 恒定估值。独立随机波动可以随有效信息积累减弱；旧报告中容量变化系统性追随模型选择的情景是前提破坏的压力实验，不代表线上自然错误率。

默认接收网站为 https://codex.nightunderfly.online/。客户端使用新迁移更新占位默认值，保留自定义地址，科研默认关闭且方法变更须重新确认授权。

同批次更新替换、不同历史批次保留；不因关闭或迁移自动撤回，也不按120天失活清理历史贡献。只有用户明确确认的签名撤回删除其远端贡献，不删除客户端原始事实。

配套客户端审阅：LingyeNBird/Sub2Pool#5。先部署接收端，再更新客户端与0051迁移，最后由管理员重新授权。所有回归使用隔离测试服务，不向生产网站发送模拟贡献。
